- args:
    sql: CREATE TABLE "product_rating"(product_id integer NOT NULL, customer_id integer
      NOT NULL, comments text NOT NULL, rating numeric NOT NULL, PRIMARY KEY ("product_id")
      )
  type: run_sql
- args:
    name: product_rating
  type: add_existing_table_or_view
