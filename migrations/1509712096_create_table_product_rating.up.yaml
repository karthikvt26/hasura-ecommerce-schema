- args:
    sql: CREATE TABLE "product_rating"(id serial NOT NULL, product_id integer NOT
      NULL, customer_id integer NOT NULL, comment text NOT NULL, rating numeric NOT
      NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: product_rating
  type: add_existing_table_or_view
