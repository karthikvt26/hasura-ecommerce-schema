- args:
    sql: CREATE TABLE "product_rating"(customer_id integer NOT NULL, product_id integer
      NOT NULL, comments text NOT NULL, rating numeric NOT NULL, id serial NOT NULL,
      PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: product_rating
  type: add_existing_table_or_view
